cmake_minimum_required(VERSION 3.15)

set(SOURCES )
set(HEADERS 
    interfaces/i-protocol.hpp
)

# add_library(core STATIC ${SOURCES} ${HEADERS})

# target_include_directories(core PUBLIC ${SRC_ROOT})

# target_link_libraries(core PUBLIC 
#     utils
#     protos
#     sound
# )
